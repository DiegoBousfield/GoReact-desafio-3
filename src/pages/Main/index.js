import React, { Fragment } from 'react';

import Map from '../../Components/Map';
import Modal from '../../Components/Modal';

const Main = () => (
  <Fragment>
    <Map />
    <Modal />
  </Fragment>
);

export default Main;
